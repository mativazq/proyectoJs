/* let nombreIngresado = prompt('ingrese nombre de usuario');
console.log (nombreIngresado);
alert ('Aloha ' + nombreIngresado);


let edad = prompt ('Aca va tu edad');
let nacimiento = 2022 - edad ;
alert ('sos categoria' + nacimiento);
 */

let numeroIngresado = prompt('Ingrese un numero');

    if (numeroIngresado > 1000) {
        alert ('el numero es mayor a 1000');
    } else {
        alert ('el numero es menor a 1000');
    }
